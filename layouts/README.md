# Layouts

This folder functions, together with `static/styles/*.css`, as the base theme for this site.

Initially I used a separate theme repo, but I don't intend to re-use this theme, so why bother?
